const output=document.querySelector('#session-detail');const id=new URLSearchParams(location.search).get('id');
AJJ.loadProgram().then(data=>{const s=data.sessions.find(item=>item.id===id)||data.sessions[0];document.title=`${s.title} — AJJ 2027`;output.innerHTML=`<div class="prototype-note" role="note"><strong>Example session</strong> This title, abstract, and speaker information are dummy content for prototype testing.</div><div class="session-hero"><div><span class="session-type">${AJJ.escape(s.type)}</span><h1>${AJJ.escape(s.title)}</h1><ul class="speaker-list">${s.speakers.map(name=>`<li>${AJJ.escape(name)} <span class="muted">· Example participant</span></li>`).join('')}</ul></div><aside class="session-facts"><dl><dt>Day</dt><dd>${AJJ.formatDay(s.start)}</dd><dt>Time</dt><dd>${AJJ.formatTime(s.start)}–${AJJ.formatTime(s.end)} JST</dd><dt>Room</dt><dd><a href="map.html"><strong>${AJJ.escape(s.room)}</strong> · View map</a></dd><dt>Format</dt><dd>${AJJ.escape(s.type[0].toUpperCase()+s.type.slice(1))}</dd></dl></aside></div><section class="abstract"><h2>Abstract</h2><p>${AJJ.escape(s.abstract)}</p></section>`}).catch(error=>output.innerHTML=`<p class="error">${AJJ.escape(error.message)}</p>`);
